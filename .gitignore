# ── Runtime data ──
data/chroma_db/
data/vector_db/
data/chunks/
data/*.pkl
data/*.npy
data/*.jsonl
data/*.json

# ── Config (may contain API keys) ──
/config.json

# ── Models (only the built-in default is tracked) ──
models/*
!models/BAAI_bge-small-zh-v1.5/

# ── Build / Release ──
release/

# ── Python ──
__pycache__/
*.pyc
venv/
*.egg-info/

# ── IDE ──
.vscode/
.idea/

# ── OS ──
.DS_Store
Thumbs.db

# ── Claude ──
.claude/
CONTINUE.md
